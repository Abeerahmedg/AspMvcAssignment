@model PeopleViewModel


@foreach (var person in Model.tempList)
{
    <div class="container">
        <div class="row">
            <div class="col">
                <h4>@person.Id</h4>
            </div>
            <div class="col">
                <h4>@person.Name</h4>
            </div>
            </div>
        </div>
    
}
